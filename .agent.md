# وضعیت پروژه، قوانین فعال و برنامه‌ی توسعه

آخرین به‌روزرسانی: ۱۲ سپتامبر ۲۰۲۶

## وضعیت پروژه

فاز جاری: MVP منوی دیجیتال کافه‌رستوران با سطح Production-Ready برای توسعه‌ی فعلی و آماده‌سازی برای فازهای بعدی.

تصمیم‌های تثبیت‌شده:

- ثبت سفارش حضوری از مسیر QR فعال است؛ Checkout، درگاه پرداخت و منطق Cart UI در این مرحله غیرفعال هستند.
- مدل‌های Cart، CartItem، Order، OrderItem و Payment در Schema مستند شده‌اند، اما Prisma در Runtime استفاده نمی‌شود.
- رزرو در UI باید بر اساس بازه‌های چندگانه‌ی قابل تنظیم از پنل ادمین باشد؛ فایل `img_3.jpg` فقط مرجع بصری طراحی است.
- دسترسی داده فقط با MongoDB Native Driver انجام می‌شود.
- نقش‌های معتبر فقط `SuperAdmin`، `Manager` و `Staff` هستند.
- واحد ذخیره‌سازی قیمت، ریال (IRR) است و در UI فقط به تومان تبدیل می‌شود.

## تکمیل‌شده

- هم‌راستا‌سازی Seed و Native Driver با قرارداد Canonical داده برای User، MenuCategory، MenuItem و Table.
- حذف استفاده از فیلدهای legacy شامل `roleId`, `scheduleStart`, `scheduleEnd`, `qrToken`, `stockCount`, `isUnlimited`, `paymentStatus`, `gateway` از مسیرهای Seed و Runtime.
- تنظیم `metadataBase` و حذف هشدارهای build مرتبط با Metadata.
- حذف هشدار NFT tracing از `src/server/media-storage.ts`.
- به‌روزرسانی `src/server/db.ts` برای افزودن دسترسی مستقیم به MongoClient و آماده‌سازی تراکنش‌های اتمیک.
- توسعه‌ی تراکنش‌محور در `src/lib/order-service.ts` با fallback امن برای محیط‌های غیرتراکنشی.
- تکمیل گزارش‌گیری مالی با شاخص‌های `costs`, `averageOrderValue`, `estimatedProfitMargin` و نمایش آن در UI.
- به‌روزرسانی ناوبری عمومی برای نمایش مسیر رزرو میز از صفحه اصلی و هدر سایت.
- احراز هویت HMAC با کوکی `pm_session`.
- RBAC برای مسیرهای مدیریتی و تزریق هدرهای هویت.
- صفحات عمومی خانه، درباره، فهرست، جزئیات محصول، رزرو میز و منوی QR.
- مدیریت منو، میزها، کاربران، تنظیمات، سفارش‌های مدیریتی، گزارش‌ها و رزروها.
- Waiter Call شامل API، SSE و acknowledge/resolve.
- Schema مستنداتی با enumها و روابط کامل آینده.
- حذف تولید خودکار Prisma Client از چرخه‌ی نصب پروژه.
- به‌روزرسانی `README.md` با مستندات پروژه، مسیرهای اصلی، استراتژی رندرینگ و دستورالعمل دیپلوی.

## وضعیت اعتبارسنجی نهایی

تأیید شده با اجراهای تازه:

- `npm run lint` ✅
- `npm run typecheck` ✅
- `npm run build` ✅

نتیجه‌ی نهایی: build با موفقیت انجام شد، هیچ warning یا error باقی نمانده است.

## کارهای باقی‌مانده به‌ترتیب اولویت

1. تکمیل Authorization مستقل در تمام APIهای مدیریتی، خارج از وابستگی به Proxy.
   - وضعیت فعلی: مسیرهای اصلی `menu`, `categories`, `tables`, `settings`, `orders`, `media/upload` با Role Check مستقل به‌روزرسانی شده‌اند.
   - باقی‌مانده: بررسی نهایی و اطمینان از هم‌خوانی با سیاست‌های Proxy در مسیرهای حاشیه‌ای و پیام‌های خطا.
2. بازطراحی رزرو میز مطابق دقیق `img_3.jpg` و تعریف بازه‌های چندگانه و قابل تنظیم از پنل ادمین.
   - وضعیت فعلی: API و UI رزرو به بازه‌های چندگانه و تنظیم‌پذیر از تنظیمات متصل شده‌اند.
   - باقی‌مانده: بازبینی UX، اعتبارسنجی نهایی و هم‌راستاسازی متن‌ها و نمایش‌ها با فایل مرجع.
3. اتمیک‌کردن ثبت سفارش/OrderItem/موجودی در Native Driver برای کاهش ریسک داده در فاز آینده.
   - وضعیت فعلی: ساختار داده و ورودی‌ها آماده هستند، اما تراکنش اتمیک و کاهش موجودی در سطح Repository/Service هنوز تکمیل نشده‌اند.
4. تکمیل گزارش فروش، سود، خرید، هزینه و پرفروش‌ترین محصولات با مدل قیمت‌گذاری واقعی.
5. بازبینی نهایی Accessibility، SEO و Design Tokenهای پراکنده در صفحات عمومی و پنل.

## قوانین اجرایی

- قبل از هر مرحله تحلیل و فهرست اولویت‌ها ثبت می‌شود.
- کد اجرایی از Prisma Client استفاده نمی‌کند.
- تغییر فایل سالم فقط با دلیل فنی مشخص انجام می‌شود.
- کامنت‌های کد فقط برای منطق غیربدیهی و به زبان فارسی نوشته می‌شوند.
- پس از هر مرحله، همین فایل با وضعیت واقعی پروژه به‌روزرسانی می‌شود.

Todoهای دقیق و اولویت‌بندی‌شده
تکمیل Authorization مستقل در تمام APIهای مدیریتی

وضعیت فعلی: در حال حاضر بخش‌های اصلی به‌صورت مستقل نقش‌محور شده‌اند.
مانده: بازبینی نهایی مسیرهای حاشیه‌ای، هم‌خوانی با Proxy و یکپارچه‌سازی پیام‌های خطا.
تکمیل رزرو میز و بازه‌های چندگانه

وضعیت فعلی: API و UI به مدل چندبازه‌ای متصل شده‌اند.
مانده: بازبینی UX، اعتبارسنجی نهایی و هم‌راستاسازی نمایش‌ها با مرجع بصری.
اتمیک‌کردن ثبت سفارش و OrderItem در Native Driver

وضعیت فعلی: ساختار داده آماده است، اما تراکنش اتمیک و مدیریت موجودی در سطح Repository/Service هنوز تکمیل نشده است.
مانده: استفاده از تراکنش MongoDB و جلوگیری از ناسازگاری داده بین Order، OrderItem و موجودی.
تکمیل گزارش‌گیری مالی

خرید، هزینه، سود، فروش و محصولات پرفروش
مدل قیمت‌گذاری واقعی برای هزینه تمام‌شده و سود ناخالص/خالص
بازبینی Accessibility، SEO و Design Tokens

حذف رنگ‌های پراکنده و استانداردسازی Design Tokens
بررسی WCAG AA و بهینه‌سازی SEO
بازبینی SSE و مقیاس‌پذیری

مدیریت قطع اتصال، heartbeat، مصرف منابع، وضعیت Event Streaming
مستندسازی نهایی و Seed/Rendering Strategy

ثبت دقیق استراتژی هر صفحه
تکمیل مستندات و تأیید نهایی Seed و قرارداد داده
