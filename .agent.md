# وضعیت پروژه، قوانین فعال و برنامه‌ی توسعه

آخرین به‌روزرسانی: ۳۱ اوت ۲۰۲۶

## وضعیت پروژه

فاز جاری: MVP منوی دیجیتال کافه‌رستوران.

تصمیم‌های تثبیت‌شده:

- ثبت سفارش MVP فقط برای سفارش حضوری از مسیر QR است؛ Checkout، درگاه پرداخت و منطق فعال Cart در UI وجود ندارد.
- مدل‌های Cart، CartItem، Order، OrderItem و Payment در Schema با روابط واقعی مستند شده‌اند، اما Prisma در Runtime استفاده نمی‌شود.
- رزرو فقط یک بازه‌ی دوساعته از ساعت ۲۰:۰۰ تا ۲۲:۰۰ دارد. فایل `img_3.jpg` فقط مرجع بصری طراحی است.
- دسترسی داده فقط با MongoDB Native Driver انجام می‌شود.
- نقش‌های معتبر فقط `SuperAdmin`، `Manager` و `Staff` هستند.

## تکمیل‌شده

- ساختار Next.js 16 و Proxy مبتنی بر `src/proxy.ts`.
- احراز هویت HMAC با کوکی `pm_session`.
- RBAC برای مسیرهای مدیریتی و تزریق هدرهای هویت.
- صفحات عمومی خانه، درباره، فهرست، جزئیات محصول و منوی QR.
- مدیریت منو، میزها، کاربران، تنظیمات و سفارش‌های مدیریتی.
- Waiter Call شامل API، SSE و acknowledge/resolve.
- Schema مستنداتی با enumها و روابط کامل آینده.
- حذف تولید خودکار Prisma Client از چرخه‌ی نصب پروژه.

## کارهای باقی‌مانده به‌ترتیب اولویت

1. هم‌راستا کردن Native Driver، TypeScript و seed با Schema جدید.
2. تکمیل گزارش فروش، سود، خرید، هزینه و پرفروش‌ترین محصولات.
3. تکمیل مدیریت موجودی و زمان‌بندی نمایش دسته‌بندی‌ها.
4. بازبینی امنیت، Accessibility، SEO، Dark/Light و Responsive.
5. اجرای typecheck، lint و build و رفع خطاهای مرتبط.

## آخرین مرحله‌ی انجام‌شده

- قرارداد Schema و صفحه‌ی رزرو شمسی پیاده‌سازی شد.
- API عمومی `/api/reservations` با کنترل ظرفیت و تداخل زمانی اضافه شد.
- API مدیریتی `/api/admin/reservations` و صفحه‌ی `/admin/reservations` اضافه شد.
- API عمومی `/api/orders` برای سفارش حضوری QR اضافه شد.
- SSE سفارش‌ها در `/api/admin/orders/stream` و اتصال آن به پنل اضافه شد.
- TypeScript با `npm run typecheck` بدون خطا اجرا شد.

## قوانین اجرایی

- قبل از هر مرحله تحلیل و فهرست اولویت‌ها ثبت می‌شود.
- کد اجرایی از Prisma Client استفاده نمی‌کند.
- تغییر فایل سالم فقط با دلیل فنی مشخص انجام می‌شود.
- کامنت‌های کد فقط برای منطق غیربدیهی و به زبان فارسی نوشته می‌شوند.
- پس از هر مرحله، همین فایل با وضعیت واقعی پروژه به‌روزرسانی می‌شود.
