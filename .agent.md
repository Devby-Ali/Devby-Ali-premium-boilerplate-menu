# .agent.md — وضعیت پروژه، قوانین فعال و تسک‌های باقی‌مانده

> آخرین به‌روزرسانی: 2026-08-27

## هویت پروژه
سیستم منوی دیجیتال کافه‌رستوران با Admin Panel کامل و RBAC سه‌نقشی.
فاز جاری: **MVP** — بدون فلوی خرید فعال در UI؛ مدل‌های Cart/Order/Payment فقط در سطح Schema فعال‌اند.

## قوانین غیرقابل مذاکره
1. **لایه داده همیشه MongoDB Native Driver** (`src/server/db.ts`). فایل `prisma/schema.prisma` فقط مستندسازی است و هرگز Runtime Source نیست.
2. **RBAC دقیقاً سه نقش**: `SuperAdmin | Manager | Staff` — هیچ نقش عمومی «Admin» مجاز نیست.
3. **Proxy** در `src/proxy.ts` (جانشین Middleware در Next.js 16) مسیرهای `/admin/:path*` و `/api/admin/:path*` را هم AuthN و هم AuthZ می‌کند؛ کوکی `pm_session` با HMAC از طریق `src/lib/auth-edge.ts`.
4. هر تصمیم فنی باید با مستندات باندل‌شده در `node_modules/next/dist/docs/` تطبیق داده شود — نه دانش قدیمی.
5. صفحه رزرو باید **تقویم شمسی** داشته باشد و مطابق مرجع بصری `img_3.jpg` باشد.
6. صفر TypeScript Error و صفر Lint Error در strict mode؛ Dark/Light و Responsive در همه صفحات.

## وضعیت پیاده‌سازی

### انجام‌شده و تأییدشده
- لایه داده Native Driver با تایپ‌های Doc هم‌راستا با schema.prisma + ایندکس‌ها (مدل‌های آینده شامل Cart/Order/Payment آماده).
- Auth سفارشی JWT-مانند (HMAC-SHA256 دستی، timing-safe compare) — کوکی `pm_session`.
- صفحات عمومی: `/` ISR(60s)، `/about`، `/list` ISR، `/product/[slug]` ISR+generateStaticParams، `/t/[token]` force-dynamic با WaiterCallButton.
- Admin: داشبورد، مدیریت منو، سفارشات (REST)، میزها (CRUD+QR روی canvas)، تنظیمات، لاگین/خروج.
- **Proxy کامل RBAC**: matcher دوگانه HTML/API، `getRolesForPath` مبتنی بر PERMISSIONS، جدول رفتاری ۴ حالته (redirect / 401 / 403 / redirect login→dashboard)، تزریق هدرهای `x-user-id/x-user-role/x-user-name`.
- QR ها اکنون به مسیر صحیح `/t/<token>` اشاره می‌کنند.
- **مدیریت کاربران واقعی**: CRUD کامل در `/admin/users` + API `/api/admin/users` (فقط SuperAdmin)، قیود LAST_SUPERADMIN/SELF_DELETE/SELF_DEMOTE، AuditLog در `user-service`.

### باقی‌مانده (به ترتیب اولویت)
1. Waiter Call Real-time کامل — اتصال SSE موجود (`/api/admin/waiter-calls/stream`) به UI پنل + فلوی acknowledge/resolve.
2. صفحه رزرو `/reservation` (CSR، تقویم شمسی با jalaali-js، بازه‌های دوساعته ۲۰–۲۲) مطابق `img_3.jpg` + API + صفحه مدیریتی `/admin/reservations`.
3. گزارش‌گیری `/admin/reports` (فروش/پرفروش‌ترین‌ها، تفکیک هفتگی/ماهانه/سالانه) با aggregation روی order_items.
4. SSE سفارشات لحظه‌ای در پنل (جایگزینی polling).
5. Housekeeping: حذف وابستگی‌های بلااستفاده (jose, @tanstack/react-query, bcrypt native)، full build، اجرای lint روی کل صفحات ادمین (قاعده react-hooks/set-state-in-effect در orders/tables نقض شده و باید اصلاح شود).

## تصمیمات معماری ثبت‌شده
- قاعده React Compiler «set-state-in-effect»: setState سنکرون در بدنه اثر ممنوع؛ اولین fetch هر صفحه CSR داخل IIFE با AbortController انجام می‌شود و refresh ها فقط از event handler ها فراخوانی می‌گردند.
- مقایسه امضای کوکی همیشه timing-safe (`timingSafeEqual` در auth-edge.ts).
- Audit actions: `user.create`, `user.update`, `user.delete`.
